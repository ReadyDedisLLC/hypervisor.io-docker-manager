services:
  actual:
    image: actualbudget/actual-server:latest
    restart: unless-stopped
    ports:
      - "${HOST_PORT_5006:-5006}:5006"
    volumes:
      - actual_data:/data

volumes:
  actual_data:
