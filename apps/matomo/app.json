{
  "name": "Matomo",
  "slug": "matomo",
  "description": "Open-source web analytics platform that gives you full ownership of your data and privacy-compliant visitor insights.",
  "icon": "icon.png",
  "category": "analytics",
  "version": "latest",
  "website": "https://matomo.org",
  "docs_url": "https://matomo.org/guides/",
  "supported_distros": [
    "ubuntu-22.04",
    "ubuntu-24.04",
    "debian-12",
    "rocky-9",
    "almalinux-9"
  ],
  "min_ram_mb": 512,
  "min_storage_gb": 10,
  "env": [
    {
      "key": "MYSQL_ROOT_PASSWORD",
      "label": "MySQL Root Password",
      "default": "",
      "required": true,
      "type": "password",
      "generate": true,
      "description": "Root password for the MariaDB database"
    },
    {
      "key": "MYSQL_PASSWORD",
      "label": "MySQL Password",
      "default": "",
      "required": true,
      "type": "password",
      "generate": true,
      "description": "Password for the Matomo database user"
    },
    {
      "key": "MYSQL_DATABASE",
      "label": "MySQL Database",
      "default": "matomo",
      "required": true,
      "type": "text",
      "description": "Name of the Matomo database"
    },
    {
      "key": "MYSQL_USER",
      "label": "MySQL User",
      "default": "matomo",
      "required": true,
      "type": "text",
      "description": "Database username for Matomo"
    },
    {
      "key": "MATOMO_DATABASE_ADAPTER",
      "label": "Database Adapter",
      "default": "mysql",
      "required": false,
      "type": "select",
      "options": ["mysql", "pdo_mysql"],
      "description": "Database adapter to use for Matomo"
    },
    {
      "key": "MATOMO_DATABASE_TABLES_PREFIX",
      "label": "Table Prefix",
      "default": "matomo_",
      "required": false,
      "type": "text",
      "description": "Prefix for Matomo database tables"
    },
    {
      "key": "MATOMO_DATABASE_PORT",
      "label": "Database Port",
      "default": "3306",
      "required": false,
      "type": "number",
      "description": "Port number for the database connection"
    }
  ],
  "ports": [
    {
      "container_port": 80,
      "default_host_port": 8095,
      "protocol": "tcp",
      "label": "Web UI",
      "configurable": true
    }
  ]
}
