services:
  postgresql:
    image: postgres:16
    restart: unless-stopped
    ports:
      - "${HOST_PORT_5432:-5432}:5432"
    env_file:
      - .env
    volumes:
      - postgresql_data:/var/lib/postgresql/data

volumes:
  postgresql_data:
