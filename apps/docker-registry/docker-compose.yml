services:
  registry:
    image: registry:2
    restart: unless-stopped
    ports:
      - "${HOST_PORT_5000:-5000}:5000"
    volumes:
      - registry_data:/var/lib/registry

volumes:
  registry_data:
