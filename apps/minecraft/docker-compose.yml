services:
  minecraft:
    image: itzg/minecraft-server:latest
    restart: unless-stopped
    ports:
      - "${HOST_PORT_25565:-25565}:25565"
    env_file:
      - .env
    tty: true
    stdin_open: true
    volumes:
      - minecraft_data:/data

volumes:
  minecraft_data:
