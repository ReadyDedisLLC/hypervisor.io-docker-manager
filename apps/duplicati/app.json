{
  "name": "Duplicati",
  "slug": "duplicati",
  "description": "Free backup software to store encrypted backups to cloud storage services and remote file servers.",
  "icon": "icon.png",
  "category": "file-storage",
  "version": "latest",
  "website": "https://www.duplicati.com",
  "docs_url": "https://docs.duplicati.com",
  "supported_distros": [
    "ubuntu-22.04",
    "ubuntu-24.04",
    "debian-11",
    "debian-12",
    "rocky-9",
    "almalinux-9",
    "centos-stream-9"
  ],
  "min_ram_mb": 256,
  "min_storage_gb": 5,
  "env": [
    {
      "key": "PUID",
      "label": "User ID",
      "default": "1000",
      "required": false,
      "type": "number",
      "description": "UID for the container user to match host file permissions"
    },
    {
      "key": "PGID",
      "label": "Group ID",
      "default": "1000",
      "required": false,
      "type": "number",
      "description": "GID for the container user to match host file permissions"
    },
    {
      "key": "TZ",
      "label": "Timezone",
      "default": "Etc/UTC",
      "required": false,
      "type": "text",
      "description": "Timezone for the container (e.g., America/New_York, Europe/London)"
    },
    {
      "key": "CLI_ARGS",
      "label": "CLI Arguments",
      "default": "",
      "required": false,
      "type": "text",
      "description": "Additional command-line arguments to pass to Duplicati server"
    }
  ],
  "ports": [
    {
      "container_port": 8200,
      "default_host_port": 8200,
      "protocol": "tcp",
      "label": "Web UI",
      "configurable": true
    }
  ]
}
