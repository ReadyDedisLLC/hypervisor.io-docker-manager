services:
  prometheus:
    image: prom/prometheus:latest
    restart: unless-stopped
    ports:
      - "${HOST_PORT_9090:-9090}:9090"
    volumes:
      - prometheus_data:/prometheus

volumes:
  prometheus_data:
