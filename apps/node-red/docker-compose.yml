services:
  node-red:
    image: nodered/node-red:latest
    restart: unless-stopped
    ports:
      - "${HOST_PORT_1880:-1880}:1880"
    volumes:
      - node_red_data:/data

volumes:
  node_red_data:
