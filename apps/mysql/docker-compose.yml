services:
  mysql:
    image: mysql:8.0
    restart: unless-stopped
    ports:
      - "${HOST_PORT_3306:-3306}:3306"
    env_file:
      - .env
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
