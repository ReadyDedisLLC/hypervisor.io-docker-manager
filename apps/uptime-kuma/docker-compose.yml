services:
  uptime-kuma:
    image: louislam/uptime-kuma:1
    restart: unless-stopped
    ports:
      - "${HOST_PORT_3001:-3001}:3001"
    env_file:
      - .env
    volumes:
      - uptime_kuma_data:/app/data

volumes:
  uptime_kuma_data:
