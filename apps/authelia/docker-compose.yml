services:
  authelia:
    image: authelia/authelia:latest
    restart: unless-stopped
    ports:
      - "${HOST_PORT_9091:-9091}:9091"
    env_file:
      - .env
    volumes:
      - authelia_config:/config

volumes:
  authelia_config:
