services:
  vaultwarden:
    image: vaultwarden/server:latest
    restart: unless-stopped
    ports:
      - "${HOST_PORT_80:-8080}:80"
    env_file:
      - .env
    volumes:
      - vaultwarden_data:/data

volumes:
  vaultwarden_data:
